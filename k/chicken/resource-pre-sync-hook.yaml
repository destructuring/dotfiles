#ManagedBy: cue

metadata:
  name: combo-breaker
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  template:
    spec:
      containers:
        - name: meh
          image: ubuntu
          command:
            - "false"
      restartPolicy: Never
kind: Job
apiVersion: batch/v1
